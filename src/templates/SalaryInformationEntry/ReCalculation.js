import React from 'react';

const ReCalculation = (props) => {
    const {
        data,
    } = props;

    return (
        <div>
            반가워용 저는 재계산이에용
            {data}
        </div>
    );
};

export default ReCalculation;