import React from 'react';

const InsertSalaryData = (props) => {
    const {
        data,
    } = props;

    return (
        <div>
            안뇽하세요
            {data}
        </div>
    );
};

export default InsertSalaryData;